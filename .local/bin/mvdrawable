#!/bin/sh
# Moves files like my_awesome_icon-xhdpi.png to drawable-xdhpi/my_awesome_icon.png
# Usage: mvdrawable [prefix]

for file in *.*
do
    [ -z $1 ] && prefix=drawable || prefix=$1
    name=${file%\.*}
    dir=$prefix-${name##*-}
    mkdir $dir
    mv $file $dir/${file%-*}.${file##*\.}
done
