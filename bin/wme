#!/bin/sh

. ~/.config/wmrc/config

[ -e "$PANEL_FIFO" ] && rm "$PANEL_FIFO"
mkfifo "$PANEL_FIFO"
st -n panel -g 214x2 -e panel &
while IFS=: read ev wid; do
    case $ev in
        18) wattr $(pfw) || wmf prev 2>/dev/null ;;
        4|7|19) wattr o $wid || wmf $wid 2>/dev/null ;;
    esac
done
