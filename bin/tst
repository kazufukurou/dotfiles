#!/bin/bash

# r="wwwxhhh"
# echo ${r%x*} ${r#*x}

# x="b"
# [ "$x" = "a" ] && a="a" b="b" c="c" || a="A" b="B" c="C"
# echo $a $b $c
# [ "$x" = "a" ] && a="a" b="b" c="c" || a="A" b="B" c="C"
# x=1
# [ "$x" -ge 1 -a "$x" -le 10 ] && echo "A" || echo "B"
[ ! -z "$1" ] && [ ! -z "$2" ] && echo "AB" || echo "CC"
arr=()
[ ${#arr[@]} -eq 0 ] && echo empty || echo notempty

