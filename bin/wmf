#!/bin/sh

. ~/bin/wmrc

if [ $# -eq 1 ]; then
    wid=$1
    chwso -r $wid
    wtf $wid
    x=$(wattr x $wid)
    y=$(wattr y $wid)
    w=$(wattr w $wid)
    h=$(wattr h $wid)
    wmp -a $((w / 2 + x)) $((h / 2 + y))
else
    echo "usage: $(basename $0) <wid>" >&2
fi

